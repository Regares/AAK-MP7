#include <stdio.h>
#include <math.h>
int main()
{
    float a, r;
    printf("Введите значение угла в градусах: ");
    scanf("%f", &a);
    if ((a <= 0) || (a >= 360))
    {
        printf("Ошибка: значения угла выходит из условия.");
    }
    else
    {
        r = a / 180;
        printf("Значение угла в рaдианах: %f π",  r);
    }
    return 0;
}
